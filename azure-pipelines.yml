
pool:
  vmImage: 'ubuntu-latest'

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'

- task: AzureCLI@2
  inputs:
    azureSubscription: 'Free Trial(dcc87da0-ebd1-425f-a58b-a08f6f1490c9)'
    scriptType: 'bash'
    scriptLocation: 'inlineScript'
    inlineScript: 'az appservice plan create -g d3_dev_01 -n MyPlan --is-linux --number-of-workers 4 --sku Free'
    #'az container create --resource-group d3_dev_01 --file container_instance.yml'
    addSpnToEnvironment: true 
